# Bharat Browser macOS Release Build Configuration (arm64 only, no R2 upload)
#
# Single-architecture arm64 release build. This is the nightly/manual CI variant
# for artifact-only branch builds where publishing to R2/CDN is not desired.
#
# Environment Variables:
#   Use !env tag to reference environment variables:
#     Example: chromium_src: !env CHROMIUM_SRC

build:
  type: release
  architecture: arm64

gn_flags:
  file: build/config/gn/flags.macos.release.gn

# Explicit module execution order
modules:
  # Phase 1: Setup
  - clean
  - git_setup
  - sparkle_setup

  # Phase 2: Patches & Resources
  - download_resources
  - resources
  - bundled_extensions
  - chromium_replace
  - string_replaces
  - series_patches
  - patches

  # Phase 3: Build
  - configure
  - compile

  # Phase 4: Sign & Package
  - sign_macos
  - package_macos

# Optional environment variables (for code signing):
#   MACOS_CERTIFICATE_NAME, PROD_MACOS_NOTARIZATION_APPLE_ID,
#   PROD_MACOS_NOTARIZATION_TEAM_ID, PROD_MACOS_NOTARIZATION_PWD
# Signing modules skip gracefully when not configured.

# Notification settings
notifications:
  slack: true
