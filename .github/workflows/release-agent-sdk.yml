name: Release Bharat Browser Agent SDK (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Agent SDK publishing is disabled."
